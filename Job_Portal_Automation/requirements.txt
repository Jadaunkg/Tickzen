# Job Portal Automation - Dependencies Reference
# All dependencies have been consolidated into the main requirements.txt file
# located in the tickzen2 root directory.
# 
# To install all dependencies for the entire project including Job Portal Automation:
# pip install -r ../requirements.txt
#
# This ensures all modules share the same dependency versions and reduces conflicts.

# Note: The following dependencies are now included in the main requirements.txt:
# - HTTP and API Communication (requests, urllib3)
# - Environment and Configuration (python-dotenv)
# - Firebase Integration (firebase-admin, google-cloud-firestore)
# - Logging and Monitoring (python-logging-loki)
# - Data Processing (pandas, numpy)
# - Utilities (python-dateutil, pytz)
# - Type Hints and Validation (pydantic)
# - Development and Testing (pytest, pytest-asyncio, pytest-cov, black, flake8, mypy)
